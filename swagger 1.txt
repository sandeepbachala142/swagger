swagger: '2.0'
info:
  title: My API
  version: 1.0.0
host: test.com
schemes:
  - https
basePath: /v1
paths:
  /getPlaceDetailsWithPlaceId:
    get:
      summary: Get Place Details
      description: Retrieves details about a place with a given ID
      produces:
        - application/json
      parameters:
        - in: query
          name: placeId
          description: The ID of the place to retrieve details for
          type: string
          required: true
      responses:
        200:
          description: Place details retrieved successfully
          schema:
            $ref: '#/definitions/PlaceDetails'
definitions:
  PlaceDetails:
    type: object
    properties:
      name:
        type: string
      address:
        type: string
      phone:
        type: string
      website:
        type: string
